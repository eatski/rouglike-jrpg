[package]
name = "town"
version = "0.1.0"
edition = "2024"

[dependencies]
terrain = { path = "../terrain" }
item = { path = "../item" }
item-data = { path = "../item-data" }
party = { path = "../party" }

[dev-dependencies]
