[package]
name = "party"
version = "0.1.0"
edition = "2024"

[dependencies]
item = { path = "../item" }
spell = { path = "../spell" }

[dev-dependencies]
item-data = { path = "../item-data" }
spell-data = { path = "../spell-data" }
