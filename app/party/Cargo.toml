[package]
name = "party"
version = "0.1.0"
edition = "2024"

[dependencies]
item = { path = "../item" }
item-data = { path = "../item-data" }
spell = { path = "../spell" }

[dev-dependencies]
spell-data = { path = "../spell-data" }
